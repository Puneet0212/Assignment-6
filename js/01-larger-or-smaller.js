var firstNumber = window.prompt("Enter the first integer:");
var secondNumber = window.prompt("Enter the second integer:");

if(firstNumber >= secondNumber){
    window.document.write("The larger number is " + firstNumber);
}
else{
    window.document.write("The larger number is " + secondNumber);
}
var number = window.prompt("Enter a number:");

window.document.write("************************COUNTDOWN************************<br>");

for(var i=number; i>=0; i--){
    window.document.write(i +"<br>");
}
